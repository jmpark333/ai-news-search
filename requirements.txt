# Flask 웹 프레임워크
Flask==3.0.0
flask-cors==4.0.0

# HTTP 요청 라이브러리
requests==2.31.0

# DuckDuckGo 검색 라이브러리 (실패 시 Tavily 대체)
duckduckgo-search==4.1.1

# Tavily Search API 라이브러리 (대체용)
tavily-python>=0.5.0

# 구글 번역 라이브러리 (googletrans 대체 - httpx 충돌 해결)
deep-translator>=1.11.0
